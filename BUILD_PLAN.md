# 빌드 로드맵 (제안서 추진 방안과 1:1)

## Phase 1 — 작법 엔진 MVP  ← 지금
**목표**: 아이디어 → 4막·24블록 설계도 생성·검증 (웹 1화면)
- [ ] `knowledge/method/24block.md`·`ontology.md` 의 `[원본]` 채우기  ← **최우선**
- [ ] `engine/schema.ts` 타입·검증기 확정
- [ ] `skills/jakbeop-engine/SKILL.md` 입력/출력 계약 확정
- [ ] `app/` 생성 (입력 폼 + 결과 타임라인)
- [ ] `/api/generate` (스킬 로드 + Anthropic 호출 + JSON 검증)
- [ ] 구조 검증기 UI (반전점·적대자 9·14·18·B스토리 8·15·22)

**산출(제안서 1단계)**: 작동하는 엔진 + 창작 사례

## Phase 2 — 현장 검증·고도화
- [ ] R1 취재 파이프라인 (데이터 소스 → 신뢰도 위계 → 블록 계측)
- [ ] **블록 단위 피드백 루프** 데이터 모델 (시청 신호 → 블록별 매핑 → 환류)
- [ ] 다중 프로젝트 저장/버전 (신인·제작사 베타)
- [ ] 품질·재현성 측정 지표 (KPI 연동)

**산출(2단계)**: 검증 리포트 · 표준 워크플로

## Phase 3 — 영상화·사업화·거버넌스 연결
- [ ] **영상화**: 24블록 → 시퀀스/콘티 export (영상 생성 모델 연계)
- [ ] **사업화**: IP 메타데이터 · 거래 스텁
- [ ] **거버넌스**: 권리·버전·저작권 메타, 수익 배분 모델

**산출(3단계)**: 국가 창작 플랫폼 (확산)

---
### 기술 메모
- 엔진 호출은 서버에서만 (API 키 노출 금지).
- 모든 생성 결과는 `validateStructure()` 통과를 기본 게이트로.
- 방법론 데이터(`knowledge/method/*`)는 코드와 분리해 버전 관리(원본 IP).
