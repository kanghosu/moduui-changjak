# 모두의 창작 (Moduui Changjak) — 작법 엔진 MVP

> 이 파일은 Claude Code가 **세션 시작 시 가장 먼저 읽어야 하는** 프로젝트 헌장입니다.

## 한 줄 정의
거장(김태원)의 창작 방법론(4막·24블록 「욕망의 레시피」, 저작권 C-2013-022120)을
AI가 활용 가능한 데이터로 구조화하여, 누구나 일정 수준 이상의 이야기를 창작·영상화·사업화하도록
지원하는 국가 창작 인프라.

## 핵심 명제 (설계 시 항상 따를 것)
- **플롯은 AI, 후크는 인간.** AI(엔진)가 구조·인과·플롯을 설계하고, 차별화·감정·후크는 창작자가 결정한다.
- **발명이 아니라 기존 자산의 확장.** 작법을 새로 만들지 않는다. 김태원 「욕망의 레시피」를 엔진으로 옮긴다.
  > ⚠️ **"검증된 방법론"이라고 쓰지 말 것.** 2026-08-28 학술 조사에서 24블록 체계를 검증한 동료심사 논문을 찾지 못했다.
  > 4막 구조 자체는 근거가 있다(Cutting 2016). 근거 상태는 `docs/PRD_v3.md` §3-2 참조. 대외 자료도 같은 규칙을 따른다.
- **Agent = LLM + harness.** 스킬은 '일반 지시'가 아니라 '거장의 판단 규칙'을 인코딩한다. 일반 작법으로 퇴화시키지 말 것.

## 지금 만드는 것 (MVP 범위)
서비스 작동 구조 4단계(① 창작 지원 → ② 영상화 → ③ 사업화 → ④ 거버넌스) 중
**① 창작 지원 = 작법 엔진** 하나를 먼저 구현한다.

흐름: `입력(아이디어/로그라인) → 작법 엔진(4막·24블록 구조 설계) → 출력(원천스토리 설계도 + 인물·욕망선) → 구조 검증`

## 아키텍처
- **Web**: Next.js (App Router, TypeScript, Tailwind) — `app/` (Claude Code가 생성)
- **Engine**: `/api/generate` 라우트가 `skills/jakbeop-engine/SKILL.md`를 컨텍스트로 로드 → Anthropic API 호출 → `engine/schema.ts`로 검증된 JSON 반환
- **Agent harness**: `.claude/agents/*` (R1~R6 역할 분담)
- **방법론 데이터**: `knowledge/method/*` (김태원 원본으로 채움)

## 반드시 지킬 원칙
1. **방법론 IP 보호** — `knowledge/method/*`의 `[원본]` 슬롯은 김태원 「욕망의 레시피」 원본/강의자료로만 채운다. 임의 창작·추정 금지.
2. **후크는 사람** — 엔진은 구조·선택지를 *제안*하고, 최종 후크·감정·디테일 결정 지점은 사용자에게 남긴다.
3. **동질화 방지** — 같은 입력이라도 차별화(후크)는 사용자 입력(hookNote)에 따라 달라지게 한다.
4. **구조 무결성 > 화려함** — 24블록의 인과·반전점·적대자 상승·B스토리가 끊기지 않게 검증한다.

## 디렉터리
- `CLAUDE.md` — 이 파일
- `BUILD_PLAN.md` — 단계별 빌드 로드맵 (제안서 추진 방안과 1:1)
- `README.md` — 착수 방법 + Claude Code에 입력할 프롬프트
- `skills/jakbeop-engine/SKILL.md` — 작법 엔진 스킬 (핵심)
- `knowledge/method/24block.md`, `ontology.md` — 방법론 데이터 (원본으로 채움)
- `engine/schema.ts` — 출력 스키마 + 구조 검증기
- `.claude/agents/` — 서브에이전트 (r1-chwijae, r2-structure, r3-character …)
- `app/` — Next.js (생성 예정)

## 시작
`README.md`의 "착수 프롬프트"를 순서대로 실행한다. 단, 그 전에 `knowledge/method/*`의 `[원본]` 슬롯을 채우는 것이 최우선이다.
