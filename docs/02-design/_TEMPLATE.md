# {feature} 설계문서

> 이 문서는 **의도**를 적는다. 코드가 무엇을 하는지가 아니라, **무엇을 해야 하는지**를 적는다.
> 갭 분석은 이 문서와 코드를 대조한다. 코드를 옮겨적으면 갭이 0이 나오고 **아무것도 검증하지 못한다.**

| 항목 | 값 |
|---|---|
| 기능 ID | `{feature}` |
| 작성 | {작성자} / {날짜} |
| 근거 문서 | `docs/PRD_v3.md` §{절} |
| 상태 | 초안 / 검수완료 |

---

## 0. Context Anchor

| Key | Value |
|---|---|
| **WHY** | 이 기능이 왜 있나. PRD v3의 어느 문장에서 나왔나 |
| **WHO** | 누가 쓰나 (신인 창작자 / 공모전 준비생 / 내부 검수자) |
| **RISK** | 이게 깨지면 무엇을 잃나 |
| **SUCCESS** | 무엇이 되면 성공인가. **측정 가능한 문장으로** |
| **SCOPE** | 이 문서가 다루는 범위의 경계 |

## 1. 기능 요구사항

**갭 분석의 대조 단위다.** 한 줄에 하나씩, 검증 가능하게 쓴다.
"잘 동작한다" 같은 문장은 금지. "무엇이 입력되면 무엇이 나온다"로 쓴다.

| ID | 요구사항 | PRD 근거 | 우선순위 |
|---|---|---|---|
| R1 | {입력} 이면 {출력} 한다 | §{절} | P0/P1/P2 |
| R2 | | | |

## 2. 비목표 (Non-goals)

**이 기능이 하지 않는 것.** 이걸 안 적으면 나중에 "왜 이건 없냐"가 갭으로 잘못 잡힌다.

- {하지 않는 것} — 이유: {왜}

## 3. 화면·컴포넌트

| 화면/컴포넌트 | 파일 | 책임 |
|---|---|---|
| {이름} | `{경로}` | {한 줄} |

**사용자 흐름**: {진입} → {단계} → {이탈/완료}

## 4. API 명세

**3자 대조 대상이다** — 이 표 ↔ 서버 라우트 ↔ 클라이언트 호출이 전부 일치해야 한다.
API가 없는 기능이면 "해당 없음"이라고 명시한다.

### `{METHOD} /api/{경로}`

| 항목 | 값 |
|---|---|
| 요청 | `{ field: type }` |
| 응답(성공) | `{ field: type }` |
| 응답(실패) | 상태코드와 형태 |
| 모델 사용 | 사용함(`MODEL_MAIN`/`MODEL_LIGHT`) / 사용 안 함 |
| 무키 폴백 | 있음(어떤 동작) / 없음 |
| 호출하는 곳 | `{클라이언트 파일:줄}` |

## 5. 데이터 모델

| 타입 | 정의 위치 | 저장 위치 | 비고 |
|---|---|---|---|
| {타입명} | `engine/schema.ts` | localStorage `{키}` | |

**저장 키**: {키 이름과 형태}
**마이그레이션**: 옛 형식이 있으면 어떻게 흡수하나

## 6. 원칙 준수 (PRD v3 §3)

이 기능이 P1~P3 원칙과 H-A~H-F 가설에 어떻게 걸리는지.

| 항목 | 이 기능에서의 의미 | 지키는 방법 |
|---|---|---|
| P1 후크는 사람 | | |
| P2 정직한 표기 | | |
| P3 되돌아갈 자유 | | |

## 7. 검증 기준

| 검증 | 방법 | 통과 조건 |
|---|---|---|
| 단위 테스트 | `tests/{파일}` | |
| 타입 | `npx tsc --noEmit` | 에러 0 |
| 빌드 | `npx next build` | 성공 |
| 실사용 | 오케스트레이터가 브라우저에서 직접 | {무엇을 눌러 무엇을 확인} |

## 8. PRD에 근거가 없는 동작 (별도 보고)

> **여기에 쓰지 말고 별도 파일 `docs/02-design/_unbacked/{feature}.md`에 쓴다.**
> 코드에는 있는데 PRD v3에서 근거를 못 찾은 동작을 적는다.
> 이것이 갭의 씨앗이다. 설계문서 본문에 넣으면 갭이 사라져 버린다.
