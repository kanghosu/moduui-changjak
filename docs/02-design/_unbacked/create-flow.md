# create-flow — PRD에 근거가 없는 동작

| # | 코드에 있는 동작 | 위치(파일:줄) | PRD에서 근거를 못 찾은 이유 | 이게 문제인가 |
|---|---|---|---|---|
| 1 | 3단계 로그라인 mock의 관계 중심 설명에 `B스토리`라는 전문 용어가 그대로 들어가 사용자에게 노출된다 | `app/api/loglines/route.ts:26-30`; `components/create/LoglineStage.tsx:62-65` | PRD는 3안의 관계 방향을 요구하지만 전문 용어를 사용할 시점은 정의하지 않는다. 초보자용 흐름과 5단계 전 전문 용어 비노출 의도에는 어긋난다 | 의도 이탈 — R11 갭이며 5단계 이전 용어 노출 |
| 2 | 장면 배치 화면은 무키 휴리스틱 응답도 `AI 분석 초안 기준`으로 표시한다 | `components/ScenePlacement.tsx:92-100`; `app/api/scene-blocks/route.ts:103-115` | PRD P2는 실제 경로를 정직하게 표기하고 휴리스틱 결과를 AI 성능처럼 보이지 않게 하라고 하지만, 이 화면의 문구별 분기 규칙은 정의하지 않는다 | 의도 이탈 — 무키 데모에서 provenance가 실제 엔진과 불일치 |
| 3 | 로그라인 재생성 시 기존 3안을 새 결과로 교체하고 선택을 `null`로 초기화한다 | `app/create/page.tsx:156-175,272` | PRD P3는 하위 산출물을 지우지 않는다고 명시하지만, 재생성 시 이전 안을 보관해야 하는지에 대한 별도 버전·목록 모델은 정의하지 않는다 | 의도 이탈 — 이전 선택지 삭제가 P3와 충돌 |
| 4 | 1단계에서 발화를 다시 제출하면 기존 답변은 빈 객체로 바꾸면서 로그라인·심화 메모·후크는 그대로 남긴다 | `app/create/page.tsx:142-147` | PRD는 되돌아갈 자유와 산출물 보존을 요구하지만, 발화를 다시 제출했을 때 이전 단계 산출물의 재계산·무효화 규칙은 정의하지 않는다 | 판단 불가 — 데이터가 서로 다른 발화에 걸쳐 섞일 수 있음 |
| 5 | 4단계 심화 축에 PRD 표의 인물·사건·흐름 외에 `자료·취재` 축을 추가한다 | `components/create/DeepenStage.tsx:8-13,51-62` | PRD §4는 인물·사건·흐름 심화를 적었고 자료·취재 버튼은 별도 기능으로 적지 않았다 | 의도된 미기재 가능 — 범위를 넓히는지 확인 필요 |
| 6 | 장면이 바뀔 때마다 `AbortController`로 이전 장면 분석을 취소하고, 후보를 최소 3개·최대 4개로 파싱한다 | `components/ScenePlacement.tsx:61-90`; `app/api/scene-blocks/route.ts:13-26` | PRD는 장면에서 후보 블록을 역산한다고만 하며 취소 시점과 후보 개수를 정하지 않는다 | 의도된 미기재 — 비동기·응답 계약의 구현 세부 |
| 7 | 장면 후보의 함수·이유에서 `24블록`, `블록`, `B-Story`, `B스토리`를 각각 `이야기 흐름`, `자리`, `동맹선`으로 치환한다 | `components/ScenePlacement.tsx:43-49,118-119,127-138` | PRD는 초보자용 구조화 흐름을 말하지만 용어 치환 사전은 명시하지 않는다 | 의도된 미기재 가능 — R11을 지키기 위한 표현 정책인지 확인 필요 |
| 8 | 이야기 지도는 초기 선택 블록을 전환점(기본 13)으로 열고, 블록 클릭 시 가운데로 부드럽게 스크롤하며 reduced-motion이면 자동 스크롤한다 | `components/create/StoryTimeline.tsx:56-99` | PRD는 Plottr식 가로 타임라인과 24칸 등폭만 요구하며 초기 선택·스크롤 동작은 적지 않았다 | 의도된 미기재 — 접근성·탐색 UX 세부 |
| 9 | 지도에 반전점 13, 적대자 상승 9·14·18, B스토리 8·15·22를 고정 앵커로 칩 표시한다 | `components/create/StoryTimeline.tsx:43-49,112-116` | PRD는 4막·24블록을 제품 방식으로 언급하지만 이 번호별 UI 앵커 표시를 화면 계약으로 정의하지 않는다 | 판단 불가 — 방법론 표현인지 단순 UI 장식인지 확인 필요 |
| 10 | 무키 로그라인 mock은 벤치마크가 부족하면 기본 제목 `기생충`을 사용하고, 관계 방향 설명에 `B스토리`를 삽입한다 | `app/api/loglines/route.ts:9-15,17-37` | PRD는 참고 벤치마크와 3가지 관점은 요구하지만 기본 제목 선택 규칙과 mock 문구는 근거가 없다 | 의도 이탈 가능 — 사용자가 언급하지 않은 작품을 추천하거나 전문 용어를 노출할 수 있음 |
| 11 | 5단계에 작업실 이동 버튼과 `스토리툰` 준비 중 카드·비활성 CTA를 함께 표시한다 | `components/create/CreateView.tsx:193-215` | PRD는 스토리툰이 아직 없다고 하지만, `/create` 결과 화면에서 준비 중 CTA를 노출하는 방식은 정의하지 않는다 | 의도된 미기재 — 전환 실험·수익모델 후보로 문서화가 필요함 |
| 12 | 음성 입력은 한국어 `ko-KR`, continuous/interim 결과를 사용하고 무음 종료 뒤 자동 재시작하며, 미지원 브라우저에서는 컴포넌트 자체를 렌더링하지 않는다 | `components/VoiceInput.tsx:42-101` | PRD §4는 Web Speech API 음성 입력만 요구하고 언어·무음 재시작·미지원 UI 정책은 정의하지 않는다 | 의도된 미기재 — 브라우저별 동작 차이를 별도 검증해야 함 |
| 13 | 1단계에 아이디어 안내 칩 5개, 글자 수 표시, `자동 저장`, 전부 지우기 확인 대화상자를 둔다 | `components/create/IdeaStage.tsx:9-15,49-60,63-86`; `app/create/page.tsx:227-237` | PRD는 자유 발화를 시작점으로 정하지만 안내 칩·문자 수·확인 대화상자 문구는 정의하지 않는다 | 의도된 미기재 — 시작 장벽 H-A 실험 변수일 수 있음 |
| 14 | 앱 헤더의 표시 이름을 `모두의 영화 창작`으로 사용하고, 탐색·글쓰기·작업실·내 서재 링크를 제공한다 | `components/create/CreateView.tsx:114-126` | PRD §12가 서비스 최종 명칭이 문서마다 혼용 중이라고 명시하며 아직 결정하지 않았다. 메뉴 구성도 `/create` 계약에 없다 | 판단 불가 — 명칭은 열린 결정이며 현재 표기는 확정된 의도로 볼 수 없음 |
| 15 | 생성 요청에 `pipeline: true`를 붙이고 서버에서 R1→R2→R3 오케스트레이션을 실행한 뒤 `trace`와 구조 검증 `issues`를 반환한다 | `app/create/page.tsx:183-203`; `app/api/generate/route.ts:79-123` | PRD는 5단계에서 이야기 지도를 생성한다고만 하고 pipeline 플래그·R1/R2/R3 내부 실행·trace 반환을 제품 계약으로 정의하지 않는다 | 의도된 미기재 — 서버 내부 구성과 외부 응답을 분리해 문서화할 필요가 있음 |
| 16 | `/api/benchmark`가 확정 라이브러리와 AI 분석 라이브러리를 합치고 TMDB 포스터를 조회해 3단계 카드에 매칭한다 | `app/api/benchmark/route.ts:85-108`; `app/create/page.tsx:55-67,89-91` | PRD는 벤치마크를 함께 보여준다고 하지만 라이브러리 병합 우선순위·TMDB 조회·포스터 매칭은 명시하지 않는다 | 의도된 미기재 — 외부 데이터 의존성과 실패 계약 확인 필요 |
| 17 | 세션 복원 시 저장된 객체를 검증 없이 `emptySession()`에 얕게 병합하고, 생성 결과 자체는 React 상태 `result`에만 둔다 | `app/create/page.tsx:45-52,31-36`; `engine/creation.ts:84-88` | PRD는 사용자 데이터 층·프로젝트·세션·스냅샷을 말하지만 현재 세션 복원 스키마와 생성 결과의 세션 저장 여부는 정의하지 않는다 | 판단 불가 — 새로고침 뒤 5단계에서 지도 복원이 끊길 가능성이 있음 |

