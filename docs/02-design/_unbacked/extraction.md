# extraction — PRD에 근거가 없는 동작

| # | 코드에 있는 동작 | 위치(파일:줄) | PRD에서 근거를 못 찾은 이유 | 이게 문제인가 |
|---|---|---|---|---|
| 1 | 휴리스틱 추출기가 공백을 합치고 문장부호로 문장을 나눈 뒤, 정규식 패턴으로 장면·주인공·사건·주제·결말·시대·장르·톤을 추출한다 | `engine/extract-heuristic.ts:13-18,21-67` | PRD는 “실제로 존재하는 내용만 추출”과 품질 측정을 요구하지만, 정규식·문장 분리·필드별 패턴은 정의하지 않는다 | 판단 불가 — 언어별 구현 선택일 수 있으나 품질 회귀의 원인이 될 수 있음 |
| 2 | 휴리스틱 추출기를 직접 호출할 때 `findBenchmark` 콜백이 없으면 벤치마크 추출을 건너뛴다 | `engine/extract-heuristic.ts:6-8,69-71` | PRD는 참고 영화 요소와 추출 품질을 언급하지만, 휴리스틱 함수의 콜백 의존성이나 직접 호출 계약은 적지 않았다 | 판단 불가 — 호출부가 항상 콜백을 주는지는 별도 계약으로 고정할 필요가 있음 |
| 3 | 실키 모델 응답에서 허용된 14개 키만 순회하고, 문자열이 아니거나 빈 문자열이거나 정확히 `없음`인 값은 버린다 | `app/api/extract/route.ts:13-16,26-35` | PRD는 요소별 4종 메타데이터와 모름 상태를 요구하지만, 허용 키 목록과 문자열 정제 방식은 정의하지 않았다 | 의도 이탈 가능 — 모름을 버리는 동작은 설계문서 R2·R6와 충돌하므로 별도 구현 갭으로도 추적해야 함 |
| 4 | 모델 JSON 파싱 또는 모델 호출이 실패하면 동일 호출을 JSON 전용 지시와 함께 한 번 재시도한다 | `app/api/extract/route.ts:84-108` | PRD는 실키 경로가 미검증이라고만 하고 재시도 횟수·재시도 문구를 정하지 않았다 | 의도된 미기재 — 운영 안정성 정책이지 제품 의도는 아님 |
| 5 | 추출 성공 응답에 `poolSize`와 `model` 필드를 추가로 반환한다 | `app/api/extract/route.ts:112-120` | PRD는 최대 질문 수와 모델 경로의 정직한 표기는 말하지만, 질문 풀 크기 공개와 모델명 반환은 요구하지 않는다 | 의도된 미기재 — 외부 계약으로 고정할지 확인 필요 |
| 6 | 잘못된 JSON은 400, 빈 발화는 400, 처리 중 예외는 500으로 보내며 한국어 고정 오류 문구를 사용한다 | `app/api/extract/route.ts:38-48,121-124` | PRD는 자유 발화와 무키 데모 범위는 정의하지만 HTTP 오류 상태와 문구는 정의하지 않는다 | 의도된 미기재 — 클라이언트 오류 처리 계약으로는 유용함 |
| 7 | 질문 화면에서 추출 결과를 편집 가능한 입력으로 보여주고, Enter 키로 다음 질문을 진행하며, `없음 · 건너뛰기` 버튼을 제공한다 | `components/create/QuestionStage.tsx:71-90,109-143` | PRD는 빈칸만 한 화면 한 질문으로 묻는다고 하지만 수정 입력·키보드 단축키·건너뛰기 UI를 각각 명시하지 않는다 | 의도된 미기재 — 사용자 통제에 도움이 되지만 제품 계약으로 볼지는 확인 필요 |
| 8 | 질문 답변을 `questionId → string`으로 저장하고, 800ms 후 `mc_session`에 자동 저장한다 | `engine/creation.ts:71-82`; `app/create/page.tsx:70-83` | PRD는 사용자 데이터 층과 localStorage→Dexie 순서를 말하지만 답변의 현재 저장 형식과 800ms 디바운스는 명시하지 않는다 | 의도된 미기재 — 저장 전략 변경 시 함께 재검토해야 함 |
| 9 | `missingQuestions`는 호출자가 넘긴 `cap`을 받지만 실제 상한은 항상 `Math.min(cap, MAX_QUESTIONS)`로 다시 제한한다 | `engine/creation.ts:51-56` | PRD는 10개 상한을 요구하지만 내부 함수의 호출자별 cap 인자와 음수·비정상 cap 처리 규칙은 정의하지 않는다 | 판단 불가 — 상한을 지키는 안전장치이나 API 계약으로는 문서화되지 않음 |

