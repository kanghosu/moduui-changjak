| # | 코드에 있는 동작 | 위치(파일:줄) | PRD에서 근거를 못 찾은 이유 | 이게 문제인가 |
|---|---|---|---|---|
| 1 | 작품 제목을 `story.title` 또는 로그라인 첫 문장에서 만들고, 문장부호에서 자르며 28자 초과를 줄이고 기본값을 사용한다. | `engine/library.ts:47-51` | PRD v3는 작품이 쌓이고 다시 열려야 한다고만 하며 제목 생성 규칙·길이·기본 문구를 정하지 않는다. | 판단: **의도된 미기재** |
| 2 | 작품에 무작위 ID, 제목, 생성·수정 시각, `origin`, 선택적 `benchmarkName`을 붙인다. | `engine/library.ts:54-67` | PRD v3는 사용자 데이터의 보존을 말하지만 이 메타데이터의 필드·ID 생성·출처 값 규칙을 정의하지 않는다. | 판단: **의도된 미기재** |
| 3 | 목록을 `updatedAt` 내림차순으로 정렬한다. | `engine/library.ts:70-73`, `129-131` | PRD v3는 다중 작품 저장과 재방문은 말하지만 정렬 기준을 말하지 않는다. | 판단: **의도된 미기재** |
| 4 | `mc_works`가 없거나 JSON이 깨졌거나 배열이 아니면 빈 목록으로 처리하고, `id`와 `story.blocks`가 없는 항목을 걸러낸다. | `engine/library.ts:75-89` | PRD v3는 데이터 소실 방지를 중요하게 다루지만 손상된 localStorage를 빈 목록으로 보여줄지, 복구·경고할지 정하지 않는다. | 판단: **판단 불가** |
| 5 | 옛 `mc_project`를 JSON으로 읽어 블록이 있는 작업만 흡수하고, 기존 목록에 같은 로그라인이 있으면 건너뛰며, 새 작품의 `origin`을 `write`로 둔다. | `engine/library.ts:91-110` | PRD v3에는 `mc_project` 마이그레이션의 존재·중복 판단·유효성 조건·출처값이 정의되어 있지 않다. | 판단: **판단 불가** |
| 6 | 앱 시작 시 `ensureMigrated()`가 한 번 실행되고, 마이그레이션된 항목을 저장하지만 옛 `mc_project` 키를 삭제하지 않는다. | `engine/library.ts:142-148` | PRD v3는 저장 전략의 순서만 정하고 레거시 키의 삭제·반복 실행·실패 처리 정책은 정하지 않는다. | 판단: **판단 불가** |
| 7 | localStorage 용량 초과 등 `writeAll`의 저장 예외를 “작품을 저장하지 못했습니다…”라는 Error로 던진다. | `engine/library.ts:119-127` | PRD v3는 localStorage의 용량·복구 한계 때문에 Dexie로 옮긴다고 하지만 현재 용량 초과 시 오류 형태와 사용자 표시 방식을 정하지 않는다. | 판단: **판단 불가** |
| 8 | 같은 ID로 저장하면 기존 항목을 갱신하고 `updatedAt`을 현재 시각으로 바꾸며, 다른 ID면 추가한다. 삭제는 ID 필터링으로 수행한다. | `engine/library.ts:129-140` | PRD v3는 다중 작품 보존을 요구하지만 동일 ID 갱신, 삭제 허용 여부, 삭제 후 복구 가능성을 정의하지 않는다. | 판단: **판단 불가** |
| 9 | 새 작품 저장 시 현재 슬롯 `mc_project`를 새 Story와 빈 `confirmed`·`snapshots`로 덮고, 현재 슬롯 저장 실패는 무시한다. | `engine/library.ts:150-164` | PRD v3는 스냅샷·하위 산출물 보존을 요구하지만 새 작품 저장 시 현재 슬롯 메타데이터를 초기화할지, 슬롯 저장 실패를 어떻게 알릴지 정하지 않는다. | 판단: **판단 불가** |
| 10 | 작품 카드에서 작업실 열기를 누르면 `mc_project`에 해당 Story와 빈 `confirmed`·`snapshots`를 기록한 뒤 `/studio`로 이동한다. | `app/library/page.tsx:46-53` | PRD v3는 작품 재방문과 버전 복원은 명시하지만 작품별 확정 상태·스냅샷을 어디에 보존하고 재진입 시 어떻게 연결할지는 명시하지 않는다. 기존 스냅샷이 이 과정에서 사라질 수 있어 P3와의 관계 확인이 필요하다. | 판단: **의도 이탈** |
| 11 | 현재 슬롯의 스냅샷만 `/library`에 표시하고, 선택한 시각의 Story로 현재 슬롯을 바꾼 뒤 복원 완료 문구를 보여준다. | `app/library/page.tsx:63-72`, `154-168` | PRD v3는 스냅샷 복원을 요구하지만 스냅샷의 소유 단위(작품인지 현재 슬롯인지), 동일 시각 처리, 복원 후 자동 이동 여부를 정하지 않는다. | 판단: **판단 불가** |
| 12 | 현재 슬롯의 `confirmed` 개수로 `n/24` 진행 상태를 표시하고, 인물 수·벤치마크·후크 존재를 카드 배지로 노출한다. | `app/library/page.tsx:74-77`, `132-146` | PRD v3에는 24블록·인물·후크가 나오지만 이 진행률 계산과 카드 배지의 표시 규칙은 없다. | 판단: **의도된 미기재** |
| 13 | `mc_draft`의 `ideaNote`·`logline` 길이를 계산해 “쓰다 만 초안”으로 보여주고, `mc_theme`로 화면 테마를 복원한다. | `app/library/page.tsx:25-44`, `171-176`, `80` | PRD v3의 사용자 데이터 목록은 프로젝트·세션·스냅샷이며, 초안 키의 길이 표시·테마 복원은 정의하지 않는다. | 판단: **의도된 미기재** |
| 14 | 초기화 중에는 빈 화면을 반환하고, 초기 localStorage JSON 오류를 모두 무시한 뒤 `ready`를 true로 만든다. | `app/library/page.tsx:25-44`, `76` | PRD v3의 P2는 정직한 표기를 요구하지만 이 화면의 로딩·오류 표시 정책은 정하지 않는다. 오류를 사용자에게 숨기는 것이 허용되는지 확인 필요하다. | 판단: **판단 불가** |

