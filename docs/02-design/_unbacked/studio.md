# studio PRD 미기재 동작 및 의도 이탈

| # | 코드에 있는 동작 | 위치(파일:줄) | PRD에서 근거를 못 찾은 이유 | 이게 문제인가 |
|---:|---|---|---|---|
| 1 | 작품 컬렉션 `mc_works`와 작업실 현재 슬롯 `mc_project`를 별도로 저장하고, 서재에서 작품을 열면 현재 슬롯을 선택한 Story로 덮어쓰면서 `confirmed`·`snapshots`를 빈 값으로 초기화한다 | `engine/library.ts:11-13,27-36`, `app/library/page.tsx:46-52` | PRD는 다중 작품과 현재 작업의 필요는 말하지만 두 저장 표현의 키·전환 시 초기화 규칙은 정의하지 않는다. 스냅샷과 확정 상태가 사라질 수 있어 P3·§9와 충돌한다 | 의도 이탈 |
| 2 | 작업실의 블록 저장·확정은 `mc_project`에만 기록하고 대응하는 `Work`를 갱신하지 않는다 | `app/studio/page.tsx:103-115`, `engine/library.ts:129-140` | §9는 작품 재방문과 “잃지 않는다”를 요구하지만 현재 작업 변경을 서재 항목에 반영하는 계약은 없다. 서재에서 다시 열면 편집 내용이 되돌아갈 수 있다 | 의도 이탈 |
| 3 | 스냅샷은 최근 10개만 유지한다 | `app/studio/page.tsx:121-124` | PRD는 실험 전 스냅샷과 언제든 복원을 요구하지만 보존 개수 상한을 정하지 않았다. 11번째 이후 오래된 버전을 복원할 수 없으므로 P3와 충돌할 가능성이 명확하다 | 의도 이탈 |
| 4 | `/library`의 스냅샷 복원도 현재 슬롯 `mc_project`만 갱신하며 `mc_works`의 작품 기록은 갱신하지 않는다 | `app/library/page.tsx:63-70` | §9의 서재 재방문·복원 요구는 작품 단위 상태 일관성을 전제하지만 복원 대상이 현재 슬롯으로만 제한된다는 근거가 없다 | 의도 이탈 |
| 5 | `/write`에서 생성한 Story는 `saveWork`를 거치지 않고 `mc_project`에만 기록된다 | `app/write/page.tsx:111-123` | PRD §4는 `/write`를 검증 비교 경로로 포함하지만 작품 저장을 현재 슬롯 단독으로 할지 다중 서재에도 넣을지 정의하지 않는다. §9의 다중 작품 요구와는 어긋난다 | 의도 이탈 |
| 6 | 작업실 블록 선택마다 `/api/suggest`를 호출하고 키가 없으면 벤치마크·장르 지침·세계 표준 기반 3개 코칭 카드를 반환하며 사용자가 일부를 초안으로 가져올 수 있다 | `app/studio/page.tsx:88-98`, `app/api/suggest/route.ts:24-105`, `components/DraftBlock.tsx:21-31` | PRD §4는 심화 메모와 후크를 말하지만 블록별 3카드 제안·세계 표준·부분 가져오기 API 계약은 정의하지 않는다 | 판단 불가 |
| 7 | 선택한 참고작을 `/api/benchmark`에서 불러온 뒤 작업실에 같은 블록의 분석을 보여주고 항상 `거장 확정`으로 표시한다 | `app/studio/page.tsx:81-86,223-227`, `app/api/benchmark/route.ts:135-144` | API는 거장 확정 라이브러리와 AI 분석 라이브러리를 모두 반환할 수 있는데 화면 표기는 origin을 검사하지 않는다. PRD §3-1 P2는 AI 분석 초안을 확정본으로 부풀리지 말라고 한다 | 의도 이탈 |
| 8 | 테마 선택을 `mc_theme`에 저장하고 오늘 작성 글자 수를 `mc_stats`에 저장해 상단에 보여준다 | `app/studio/page.tsx:22-29,55-78,165-180` | PRD는 창작자 통제와 저장·복원을 말하지만 테마 종류·일일 글자 수·별도 키의 제품 요구는 정의하지 않는다 | 의도된 미기재 |
| 9 | `DraftBlock`은 원문·AI 제안을 나란히 보여주고 일부 가져오기를 제공하지만 `/studio`에서는 사용하지 않고 `/design`의 미리보기에 연결되어 있다 | `components/DraftBlock.tsx:1-36`, `components/DesignPreview.tsx:5,212` | PRD는 사용자 편집·통제 원칙은 말하지만 이 컴포넌트의 연결 위치는 정하지 않는다. 작업실에서의 원문 보호 계약을 이 컴포넌트가 보장한다고 가정할 수 없다 | 판단 불가 |
| 10 | `ensureMigrated`는 옛 `mc_project`를 `mc_works`로 복사하고 같은 로그라인이면 중복으로 보지 않으며, 복사된 작품의 origin을 `write`로 기록한다 | `engine/library.ts:91-110,142-148` | PRD §9는 기존 작업을 잃지 않는 목적은 지지하지만 로그라인 동일성·origin 값·마이그레이션 반복 정책은 정의하지 않는다 | 의도된 미기재 |
| 11 | 서재에서 작품 삭제 전에 확인을 받고 `localWorkStore.remove`로 컬렉션에서만 삭제하며 현재 `mc_project`는 지우지 않는다 | `app/library/page.tsx:55-61` | PRD는 되돌아갈 자유를 말하지만 작품 삭제의 확인·영구성·현재 슬롯 처리 규칙은 정하지 않는다. 삭제된 작품을 작업실에서 계속 열 수 있는 상태도 설명되지 않는다 | 판단 불가 |

