# 모두의 창작 — 실행 안내 (한국어)

> 다른 컴퓨터(노트북)에서 이 프로그램을 실행하는 방법입니다. 5분이면 됩니다.

## 준비물 (한 번만)
1. **Node.js 설치** — https://nodejs.org 접속 → 초록색 버튼(LTS) 다운로드 → 계속 "다음"만 눌러 설치

## 실행 방법
1. 이 폴더(코드)를 노트북에 내려받습니다
   - GitHub에서: 초록색 **`<> Code`** 버튼 → **Download ZIP** → 압축 풀기
2. 폴더 안에서 **주소창에 `cmd` 입력 후 엔터** (그 폴더 위치에서 명령창이 열립니다)
3. 아래 두 줄을 순서대로 입력:

```
npm install
npm run dev
```

4. 크롬에서 **http://localhost:3000** 접속 → 끝! 🎬

## 시연 동선 추천 (김태원 감독님용)
1. 포스터에서 **명량** 클릭 → 감독님이 직접 확정하신 24블록이 그대로 열림
2. **"이 뼈대로 내 이야기 만들기"** → 작성 패널에서 **🎲 예시로 채우기** → **설계도 생성**
3. 결과에서 **"작업실에서 다듬기"** → 블록 13(전환점) 클릭 → 제안 3카드(기계적 대입·창조적 변주·장르 지침) 확인
4. 우측 상단 **☀️/🌙** 로 라이트/다크 전환, 작업실에선 **책상 분위기**(세피아·심야 등) 전환

## 참고
- 인터넷이 되면 영화 포스터가 자동으로 뜹니다 (TMDb)
- `.env.local` 파일에 `ANTHROPIC_API_KEY`를 넣으면 어떤 영화든 AI 자동 분석 + 실제 생성이 켜집니다 (없어도 라이브러리 24편 + 구조 골격은 전부 동작)
- 방법론 데이터는 김태원 「욕망의 레시피」 (저작권 C-2013-022120) — **저장소는 반드시 Private(비공개)로 유지**
