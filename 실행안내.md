# 모두의 영화 창작 — 실행 안내 (한국어)

> 다른 컴퓨터(노트북)에서 이 프로그램을 실행하는 방법입니다. 5분이면 됩니다.

## 준비물 (한 번만)
1. **Node.js 설치** — https://nodejs.org 접속 → 초록색 버튼(LTS) 다운로드 → 계속 "다음"만 눌러 설치

## 실행 방법
1. 이 폴더(코드)를 노트북에 내려받습니다
   - GitHub에서: 초록색 **`<> Code`** 버튼 → **Download ZIP** → 압축 풀기
2. 폴더 안에서 **주소창에 `cmd` 입력 후 엔터** (그 폴더 위치에서 명령창이 열립니다)
3. 아래 두 줄을 순서대로 입력:

```
npm install
npm run dev
```

4. 크롬에서 **http://localhost:3000** 접속 → 끝! 🎬 (포트가 쓰이고 있으면 3001·3005 등으로 자동 변경 — 명령창에 뜬 주소로 접속)

## 화면 구성
- **탐색(홈)** — 영화 100편 라이브러리. 위쪽 "거장 확정 24편"(김태원 감독 직접 분석), 아래 "AI 분석 76편"(흥행작을 같은 24블록 틀로 AI가 분석). 포스터 누르면 그 영화 뼈대가 열립니다.
- **글쓰기(/write)** — 작가의 책상. 아이디어 노트 → 로그라인·주인공 → 24블록 설계.
- **작업실(/studio)** — 블록 하나하나 다듬기. 취향대로 책상 분위기(원고지·세피아·심야·숲·타자기).
- 우측 상단 **☀️/🌙** 라이트/다크 전환. "모두의 영화 창작" 로고 누르면 언제나 홈으로.

## 시연 동선 추천 (김태원 감독님용)
1. 포스터에서 **명량** 클릭 → 감독님이 직접 확정하신 24블록이 그대로 열림 (전환점 13 강조)
2. 영화 상세에서 **🎥 감독의 기법** 확인 → **"이 뼈대로 내 이야기 만들기"** → 글쓰기 책상으로 이동
3. **🎲 예시로 채우기** → **✨ 24블록 설계도 만들기** → 결과에서 **"작업실에서 다듬기"**
4. 작업실에서 **블록 13(전환점)** 클릭 → 벤치마크 같은 블록 참고 + 제안 3카드 확인
5. **AI 분석 76편** 중 하나(예: 올드보이·인셉션) 눌러 감독 기법·24블록이 자동 채워진 것 보여주기

## 참고
- 인터넷이 되면 영화 포스터가 자동으로 뜹니다 (TMDb 키가 코드에 내장되어 있어 별도 설정 불필요)
- `.env.local` 파일에 `ANTHROPIC_API_KEY`를 넣으면 라이브러리에 **없는** 영화도 실시간 AI 분석이 켜집니다 (없어도 100편 + 구조 골격은 전부 동작)
- 방법론 데이터는 김태원 「욕망의 레시피」 (저작권 C-2013-022120) — **저장소는 반드시 Private(비공개)로 유지**
